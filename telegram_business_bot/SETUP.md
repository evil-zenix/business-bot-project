# 🚀 Инструкция по настройке Telegram Business бота

## 📋 Содержание
1. [Требования](#требования)
2. [Установка](#установка)
3. [Настройка](#настройка)
4. [Подключение к Telegram Business](#подключение-к-telegram-business)
5. [Запуск](#запуск)
6. [Использование](#использование)
7. [Решение проблем](#решение-проблем)

---

## Требования

- Python 3.9 или выше
- Telegram аккаунт с Telegram Business (Premium подписка)
- Токен бота от @BotFather

---

## Установка

### 1. Клонирование/создание проекта

```bash
mkdir telegram_business_bot
cd telegram_business_bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или установите вручную:

```bash
pip install aiogram apscheduler aiosqlite python-dotenv
```

### Для Termux (Android):

```bash
pkg update && pkg upgrade
pkg install python git
pip install aiogram apscheduler aiosqlite python-dotenv
```

---

## Настройка

### 1. Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "My Business Assistant")
4. Введите username бота (например: "my_business_assistant_bot")
5. Скопируйте полученный токен

### 2. Получение вашего ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте `/start`
3. Скопируйте ваш ID

### 3. Создание .env файла

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Откройте `.env` и заполните:

```env
BOT_TOKEN=ваш_токен_от_botfather
ADMIN_IDS=ваш_id,id_второго_админа
```

**Пример:**
```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-123456
ADMIN_IDS=123456789
```

Если админов несколько, указывайте через запятую:
```env
ADMIN_IDS=123456789,987654321,555666777
```

---

## Подключение к Telegram Business

### ⚠️ Важно: Telegram Business доступен только с Premium подпиской!

### Шаги подключения:

1. **Откройте Telegram** (мобильное приложение или десктоп)

2. **Перейдите в настройки:**
   - Нажмите на три полоски (☰) → **Settings**
   - Или нажмите на своё фото профиля

3. **Найдите Telegram Business:**
   - Прокрутите вниз
   - Нажмите **"Telegram Business"**

4. **Подключите бота:**
   - Нажмите **"Connected bots"**
   - Нажмите **"Connect bot"**
   - Найдите вашего бота по username
   - Нажмите **"Connect"**
   - Подтвердите разрешения

5. **Настройте права:**
   - Убедитесь, что бот может:
     - ✅ Read messages (читать сообщения)
     - ✅ Send messages (отправлять сообщения)

### Что происходит после подключения?

- Клиенты будут писать в **ваш личный чат** (не боту!)
- Бот будет **получать копии этих сообщений**
- Бот будет **автоматически отвечать** при совпадении триггеров
- Ответы будут идти **от вашего имени** (от бизнес-аккаунта)

---

## Запуск

### Простой запуск:

```bash
python main.py
```

### Запуск с логами в файл:

```bash
python main.py > bot.log 2>&1
```

### Запуск в фоне (Linux/Mac):

```bash
nohup python main.py &
```

### Запуск в Termux:

```bash
python main.py
```

Для фонового режима в Termux:
```bash
termux-wake-lock  # Предотвращает засыпание
nohup python main.py &
```

### Проверка работы:

Если всё настроено правильно, в консоли появится:
```
INFO - База данных инициализирована
INFO - Scheduler запущен
INFO - Бот запущен
INFO - Для доступа к админ-панели отправьте /admin
```

---

## Использование

### 🔧 Админ-панель

1. **Открыть админ-панель:**
   - Отправьте боту `/admin`

2. **Добавить сценарий:**
   - Нажмите "➕ Добавить сценарий"
   - Выберите тип триггера:
     - **Точная фраза** - бот отвечает только на точное совпадение (например, "привет")
     - **Содержит слово** - бот отвечает, если в сообщении есть слово (например, "цена")
     - **Callback кнопки** - бот отвечает на нажатие inline-кнопки
   - Введите текст триггера
   - Введите текст ответа (поддерживается HTML: `<b>жирный</b>`, `<i>курсив</i>`)
   - Добавьте кнопки (опционально)
   - Настройте напоминание (опционально)

3. **Управление сценариями:**
   - **Список сценариев** - просмотр всех сценариев
   - **Редактировать** - изменение существующего сценария
   - **Удалить** - удаление сценария
   - **Вкл/Выкл** - временное отключение без удаления

### 📝 Примеры сценариев

#### Пример 1: Автоответ на "привет"
```
Тип: Точная фраза
Триггер: привет
Ответ: Здравствуйте! 👋 Чем могу помочь?
```

#### Пример 2: Информация о ценах
```
Тип: Содержит слово
Триггер: цена
Ответ: 
💰 <b>Наши цены:</b>

Базовая консультация: 2000₽
Полный пакет: 5000₽
Premium: 10000₽

Для записи нажмите кнопку ниже!

Кнопки:
[Записаться] → callback: book_appointment
```

#### Пример 3: Расписание с напоминанием
```
Тип: Содержит слово
Триггер: расписание
Ответ: 
📅 Работаем Пн-Пт: 9:00-18:00
Напоминание: Да, через 1440 минут (24 часа)
```

### 🔔 Напоминания

Напоминания отправляются автоматически через указанное время после первого ответа.

**Примеры использования:**
- 60 минут = 1 час
- 1440 минут = 24 часа (сутки)
- 4320 минут = 3 дня

**Сценарий:**
Клиент спрашивает о записи → бот отвечает "Записали вас!" → через 24 часа: "Напоминаем о завтрашней встрече!"

---

## Решение проблем

### Бот не запускается

**Ошибка: `BOT_TOKEN не найден`**
```bash
# Проверьте наличие .env файла
ls -la .env

# Проверьте содержимое
cat .env

# Убедитесь, что нет пробелов вокруг =
BOT_TOKEN=токен  # ✅ правильно
BOT_TOKEN = токен  # ❌ неправильно
```

**Ошибка: `ADMIN_IDS не найден`**
```bash
# Добавьте свой ID в .env
echo "ADMIN_IDS=ваш_id" >> .env
```

### Бот не получает сообщения от клиентов

1. **Проверьте подключение к Business:**
   - Telegram → Settings → Telegram Business → Connected bots
   - Бот должен быть в списке

2. **Проверьте права бота:**
   - Бот должен иметь права на чтение и отправку сообщений

3. **Проверьте логи:**
   ```bash
   # В логах должно быть:
   INFO - Business connection: ...
   ```

4. **Проверьте Premium подписку:**
   - Telegram Business работает только с Premium!

### Бот не отвечает на сообщения

1. **Проверьте сценарии:**
   ```bash
   # Отправьте боту /admin
   # Нажмите "Список сценариев"
   # Убедитесь, что есть активные сценарии
   ```

2. **Проверьте триггеры:**
   - Триггер "привет" != "Привет" (регистр не важен, но пробелы важны!)
   - Используйте тип "Содержит слово" для гибкости

3. **Проверьте логи:**
   ```bash
   # В логах должно быть:
   INFO - Бизнес-сообщение от ...
   INFO - Найден сценарий ID=...
   INFO - Ответ отправлен успешно
   ```

### Напоминания не отправляются

1. **Проверьте scheduler:**
   ```bash
   # В логах при запуске:
   INFO - Scheduler запущен
   ```

2. **Проверьте настройки сценария:**
   - Напоминание: Да
   - Задержка: > 0 минут

3. **Проверьте логи:**
   ```bash
   # После отправки ответа:
   INFO - Запланировано напоминание через X мин
   ```

### База данных повреждена

```bash
# Удалите старую БД
rm scenarios.db

# Перезапустите бота (создаст новую)
python main.py
```

### Ошибки в Termux

**Ошибка: `pkg: command not found`**
```bash
apt update && apt upgrade
apt install python git
```

**Ошибка при установке зависимостей:**
```bash
# Установите build-tools
pkg install python build-essential

# Попробуйте снова
pip install -r requirements.txt
```

---

## 📊 Структура базы данных

Бот автоматически создаёт файл `scenarios.db` со следующими таблицами:

### Таблица `scenarios`:
- `id` - Уникальный ID сценария
- `trigger_type` - Тип триггера (exact/contains/callback)
- `trigger_value` - Значение триггера
- `response_text` - Текст ответа
- `keyboard_json` - JSON кнопок
- `is_reminder` - Это напоминание? (0/1)
- `reminder_delay_min` - Задержка в минутах
- `active` - Активен? (0/1)
- `created_at` - Дата создания

### Таблица `business_connections`:
- `id` - ID записи
- `business_connection_id` - ID подключения
- `user_id` - ID пользователя
- `can_reply` - Может отвечать? (0/1)
- `created_at` - Дата подключения

### Таблица `reminder_history`:
- `id` - ID записи
- `scenario_id` - ID сценария
- `chat_id` - ID чата
- `business_connection_id` - ID подключения
- `sent_at` - Время отправки

---

## 🎯 Советы по использованию

1. **Начните с простых сценариев:**
   - "привет" → "Здравствуйте!"
   - "расписание" → Ваше расписание
   - "цена" → Ваши цены

2. **Используйте "Содержит слово" для гибкости:**
   - Клиент может написать: "Какие цены?", "Сколько стоит?", "прайс"
   - Триггер "цена" сработает на всё это

3. **Добавляйте кнопки для навигации:**
   - После основного ответа добавьте кнопки "Узнать больше", "Записаться", "Контакты"

4. **Используйте HTML форматирование:**
   ```html
   <b>Жирный текст</b>
   <i>Курсив</i>
   <code>Код</code>
   <a href="https://example.com">Ссылка</a>
   ```

5. **Тестируйте триггеры:**
   - Создайте тестовый чат
   - Отправьте разные варианты фраз
   - Проверьте, что бот отвечает правильно

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте .env файл
4. Проверьте подключение к Telegram Business
5. Проверьте права бота

---

## 🔄 Обновление

```bash
# Остановите бота (Ctrl+C)

# Сделайте backup базы данных
cp scenarios.db scenarios.db.backup

# Обновите код (если нужно)
git pull  # если используете git

# Установите новые зависимости (если добавились)
pip install -r requirements.txt

# Запустите снова
python main.py
```

---

## ✅ Чеклист для запуска

- [ ] Python 3.9+ установлен
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Бот создан через @BotFather
- [ ] Токен получен
- [ ] ID получен через @userinfobot
- [ ] Файл .env создан и заполнен
- [ ] Telegram Premium подписка активна
- [ ] Бот подключён в Telegram Business
- [ ] Бот запущен (`python main.py`)
- [ ] Команда /admin работает
- [ ] Сценарии добавлены
- [ ] Тестовое сообщение от клиента обработано

---

Готово! 🎉 Ваш Telegram Business бот настроен и готов к работе!
